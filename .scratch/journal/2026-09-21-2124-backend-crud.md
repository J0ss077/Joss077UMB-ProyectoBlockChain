# Sesión 2026-09-21 21:24. Backend CRUD y historial de acciones

### 2026-09-21 21:24. Se estrena el historial de acciones

- **Tarea**: crear el mecanismo para que los agentes registren sus acciones.
- **Acciones**:
    - Creado `docs/agents/journal.md` con la convención: un archivo por sesión, anexar si la última entrada es de hace menos de dos horas, y crear uno nuevo si pasaron dos horas, si el usuario cierra la sesión o si cambia el agente.
    - Creado `.scratch/journal/` con este archivo.
    - Actualizados `AGENTS.md` y `.scratch/README.md` para apuntar a la convención.
- **Decisiones**: el registro vive en `.scratch/`, que es el área de trabajo de los agentes, para que no se filtre al material de entrega. Se registran solo acciones con efecto, confirmado por el usuario; las lecturas no se anotan.
- **Verificación**: pendiente de comprobar que los enlaces entre los tres archivos resuelvan.
- **Pendiente**: este archivo arranca a las 21:24, aunque la sesión venía en curso desde antes. Lo anterior a esta hora se resume en la entrada siguiente.

### 2026-09-21 21:24. Resumen de la parte previa de la sesión, registrado a posteriori

Se anota al final porque el mecanismo de registro todavía no existía. El detalle está en el historial de git.

- **Tarea**: analizar el documento de entrega de la Fase 2 y ajustar el repositorio en consecuencia.
- **Acciones**:
    - Eliminado `docs/entregas/` y corregidas sus referencias en `README.md`, `AGENTS.md`, `docs/README.md` y el ADR-0003, porque el usuario maneja las entregas por fuera del repositorio.
    - Ampliado `CONTEXT.md` con el caso de aplicación del sector solidario: organización, asociado, tesorero, aporte, retiro y saldo.
    - Creado el ADR-0004 con el modelo de saldos, aportes y retiros.
    - Creado `docs/referencias.md` con la bibliografía y la ubicación de los diagramas UML.
    - Creado y luego eliminado el ADR-0005, y recreado después con el tipo de arquitectura.
    - Creado `.tmp_arquitectura-software.md` con las seis subsecciones que estaban vacías en el documento de entrega.
    - Actualizado `docs/arquitectura.md` con las mismas secciones.
- **Decisiones**:
    - La estructura del esquema SQL queda congelada. El ADR-0004 se reescribió para descartar cualquier alternativa que exigiera modificarla.
    - Se descartó el contenido de las secciones vacías del DOCX, y después se regeneró a pedido del usuario.
    - El registro de redacción de la documentación pasó a un estilo directo, sin guiones largos ni giros de relleno.
    - El diagrama de arquitectura del archivo temporal pasó de ASCII a PlantUML.
- **Verificación**: los enlaces internos de todos los archivos markdown resuelven; `database/` conserva sus marcas de tiempo originales de las 18:54.
- **Pendiente**: la integración de los modelos y las conclusiones del documento de entrega siguen sin desarrollarse.

### 2026-09-21 21:29. Backend CRUD en src/ y entorno de Python

- **Tarea**: crear un backend semi-funcional para probar la base, con CRUD manual.
- **Acciones**:
    - Creado `pyproject.toml` con la dependencia `psycopg`, el layout de paquetes y la configuración de ruff y pytest.
    - Creado `src/` con `config.py`, `wallet.py`, `transaction.py`, `block.py`, `blockchain.py`, `main.py` y el subpaquete `persistence/` con `ports.py`, `connection.py`, `repositories.py` y `factory.py`.
    - Creado `.venv/` e instaladas las dependencias: psycopg 3.3.6 sobre Python 3.14.7.
    - Añadidos los objetivos `venv` y `backend` al `Makefile`.
    - Documentado el uso en `docs/puesta-en-marcha.md`. Actualizados `docs/arquitectura.md` y `AGENTS.md`.
- **Decisiones**:
    - Solo CRUD, sin reglas de negocio: no se calculan hashes, no se firma, no se valida el saldo y no se verifica la cadena. Confirmado por el usuario.
    - La persistencia queda detrás de los protocolos de `persistence/ports.py`, de modo que `blockchain.py` no importa psycopg. El límite del ADR-0005 se cumple desde el primer archivo en lugar de quedar como intención.
    - La capa de persistencia no repite los `CHECK`. Los errores de PostgreSQL se propagan tal cual, que es lo que interesa ver al probar la base.
    - Sin DDL en ningún punto: el esquema está congelado.
- **Verificación**:
    - `ruff check src` pasa sin observaciones, después de mover `Iterator` y `Sequence` a `collections.abc` y de cambiar `TransactionStatus` a `StrEnum`.
    - `python -m compileall src` compila sin errores.
    - Cargar `src.blockchain` no importa psycopg, lo que confirma el límite entre dominio y persistencia.
    - `python -m src.main` lee `DATABASE_URL` desde `.env` y falla con el mensaje de conexión rechazada, que es el comportamiento esperado sin base levantada. Eso valida el lector de `.env` y el manejo de errores.
    - Los enlaces internos de todos los archivos markdown resuelven.
    - **Sin comprobar**: la ejecución contra una base real. `podman` no arranca en el entorno donde se escribió el código, así que la prueba de punta a punta queda pendiente del lado del usuario.
- **Pendiente**: las reglas de negocio y la capa de presentación, listadas en `docs/arquitectura.md`.

### 2026-09-21 21:55. Verificación de punta a punta, completada por el desarrollador

- **Tarea**: ninguna. El desarrollador corrió los comandos del entorno y reportó la salida.
- **Acciones**: ninguna sobre el repositorio. La entrada se registra porque corrige el estado de verificación de la entrada anterior.
- **Verificación**:
    - `make venv` reconstruyó el entorno y reinstaló el paquete en modo editable sin errores.
    - `make up` levantó el contenedor y publicó el puerto.
    - `make backend` devolvió `PostgreSQL: 16.15`, `wallets: 3`, `blocks: 2`, `transactions: 3`.
    - Los tres conteos coinciden con el seed de `02_seed.sql`, así que la conexión, la resolución de `DATABASE_URL` desde `.env` y la capa de lectura quedan comprobadas contra una base real. El punto que la entrada de las 21:29 dejó sin comprobar queda cerrado.
    - Sigue sin ejercitarse la escritura: no se ejecutó ninguna creación, actualización ni borrado, ni se provocó un error de constraint.
- **Pendiente**: probar el camino de escritura y el rechazo de los `CHECK`, que es donde la base hace valer las reglas.

### 2026-09-21 22:06. Cierre de la sesión

- **Tarea**: dejar concreta la salida esperada del backend en la documentación y cerrar la sesión de trabajo.
- **Acciones**:
    - `docs/puesta-en-marcha.md`: la comprobación de conexión ahora muestra la salida exacta en lugar de describirla, con la aclaración de que la versión del servidor cambia si se actualiza la imagen.
- **Decisiones**: ninguna nueva. Se aplicó una mejora que el agente propuso y el usuario confirmó.
- **Verificación**: los conteos documentados son los que el desarrollador observó al correr `make backend`.
- **Cierre**: el usuario pidió cerrar la sesión. La próxima sesión de trabajo abre un archivo nuevo en `.scratch/journal/`, según la convención, aunque no hayan pasado dos horas. Este archivo no se vuelve a tocar.
- **Pendiente**: probar el camino de escritura y el rechazo de los `CHECK`, más las reglas de negocio y la capa de presentación listadas en `docs/arquitectura.md`.

### 2026-09-21 22:17. Accesibilidad de los pendientes, y cierre definitivo

- **Tarea**: comprobar si los pendientes quedaban accesibles para los siguientes agentes, y corregir lo que faltara.
- **Hallazgo**: no del todo. Estaban repartidos en cuatro lugares sin un punto de entrada común; `AGENTS.md` no apuntaba a ninguno; el historial no sirve como backlog porque es cronológico; y dos pendientes solo existían en el historial y en la conversación.
- **Acciones**:
    - Creado `.scratch/backend/` con `spec.md` y seis tickets: refrendo de los ADR, prueba del camino de escritura, hash y agrupación, firma, verificación de la cadena, y capa de presentación.
    - `AGENTS.md`: agregada la fila de `.scratch/` al mapa de documentos como primer punto de entrada, y una convención que obliga a revisar los tickets abiertos al empezar y a registrar como ticket lo que quede pendiente.
    - `docs/agents/journal.md`: agregada la sección de promoción de pendientes, para que un pendiente accionable no se quede solo en el historial.
    - Eliminado `.tmp_arquitectura-software.md`, que ya se había volcado al documento de entrega.
- **Decisiones**:
    - El backlog vive en `.scratch/`, no en un archivo aparte, para no crear una segunda fuente de verdad.
    - El documento de entrega y sus secciones quedan completamente fuera del repositorio. No se registran acá.
- **Verificación**: los enlaces internos resuelven y todos los tickets abiertos declaran su `Status:`.
- **Cierre**: la sesión termina acá. La próxima abre un archivo nuevo en `.scratch/journal/`. Esta entrada reemplaza como cierre a la de las 22:06, que se escribió antes de esta corrección.
- **Pendiente**: los seis tickets de `.scratch/backend/` y el de `.scratch/modelo-de-saldos/`.
